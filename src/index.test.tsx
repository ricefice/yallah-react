import { Hider } from './components/Hider/Hider'
import { Loader } from './components/Loader/Loader'

describe('Hider', () => {
  it('is truthy', () => {
    expect(Hider).toBeTruthy()
  })
})

describe('Loader', () => {
  it('is truthy', () => {
    expect(Loader).toBeTruthy()
  })
})
